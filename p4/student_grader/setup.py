"""
Lets the student_grader be installed in editable mode
through pip
"""

from setuptools import setup, find_packages

setup(
    name="student_grader",
    version="0.0",
    packages=find_packages(),
)
